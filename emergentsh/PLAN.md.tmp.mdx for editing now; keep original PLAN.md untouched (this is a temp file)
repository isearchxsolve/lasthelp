# PLAN.md - CLONE OF EMERGENT.SH USING NVIDIA NIM AS INFERENCE ENGINE (DRAFT FOR REWRITE, DO NOT USE AS-FINAL)⸻## Requirements**Functional Requirements**1. Multi-Agent AI System (Architect, Designer, Developer, Integration, PM Agents) orchestrated via FastAPI orchestrator.2. Next.js Frontend with React components for chat, live preview, dashboard, version history.3. FastAPI Backend: REST APIs for agent orchestration, task queue, file operations, GitHub integration, deployment triggers.4. NVIDIA NIM Integration: OpenAI-compatible `/v1/chat/completions` endpoint using NIM inference microservice (e.g., `meta/llama-3.1-8b-instruct`).5. MongoDB for persistence (users, projects, chats, agent states, file metadata).6. Redis for pub/sub (agent events, task queue), WebSocket connections, and session caching.7. Docker Compose for local dev (Next.js, FastAPI, MongoDB, Redis, NIM container).8. GitHub OAuth integration for repo sync and PR creation.9. One-click deploy (Vercel/Netlify/Vercel-like preview URLs) with preview URL generation.10. Credit-based usage tracking (tokens consumed via NIM API).11. Self‑debugging loop: test runner, error capture, fix generation, iteration limits.12. Real‑time WebSocket updates for chat, live preview, dashboard metrics.13. Admin dashboard for usage, credits, agent performance, system metrics.14. User auth via NextAuth.js (Google/GitHub).⸻## Non‑Functional Requirements**1. Scalable: Horizontal scaling via Docker Compose/Kubernetes; NIM horizontally scalable.2. Maintainable: Clean separation (agents, API, UI, infra). Clear interfaces via Pydantic models/FastAPI dependency injection.3. Auditable: Full audit logs (agent decisions, file changes, API calls).4. Secure: JWT auth, HTTPS, input sanitization, rate limiting.5. Detectable: Prometheus metrics exposed (`/metrics`), health checks (`/health`).6. Portable: Docker compose for local dev; Helm chart (optional) for k8s.⸻## Risks**Technical Risks**1. NIM latency/variability: Mitigate with async calls, timeouts, retries, fallback models.2. Agent hallucination/loops: Mitigate with max iteration limits, automated test validation, human‑in‑the‑loop approve/reject.3. State drift: Mitigate with event‑sourced agent state snapshots in MongoDB, replay‑able logs.4. WebSocket scale‑out: Use Redis pub/sub for horizontal scaling.5. Token cost overruns: Per‑user token counters, alerts, soft/hard limits.6. Docker compose drift: Pin exact image versions; use `--pull` and hash‑checked volumes.⸻## Acceptance Criteria**1. **[Chat Interface]** User can start a new project via chat (“Build a SaaS dashboard with Stripe”) and get a structured plan from the Architect agent.2. **[Agent Orchestration]** Orchestrator dispatches tasks to specialized agents (Architect→Designer→Developer→Integration→PM) and waits for completion signals via Redis/WebSocket.3. **[File Generation]** Agents generate correct file trees (frontend/, backend/, etc.) with syntactically valid code (Next.js pages, FastAPI routes, MongoDB models).4. **[Preview URL]** Upon completion, system triggers a one‑click deploy (Vercel‑like preview URL) and returns a clickable preview link in chat.5. **[Credit System]** Each agent call deducts tokens based on NIM API usage; users see real‑time credit deduction and remaining balance in dashboard.6. **[Self‑Debug Loop]** If generated code fails tests or lint, agent attempts self‑fix up to 3 iterations before escalating to human.7. **[GitHub Sync]** Completed project can be pushed to user’s GitHub repo via OAuth with a pull request.8. **[Health & Metrics]** `/health` endpoint returns 200; `/metrics` exposes Prometheus metrics (agent_latency, token_usage, error_rate).9. **[Docker Compose]** `docker compose up --build` spins up Next.js, FastAPI, MongoDB, Redis, and a NIM container (`nvcr.io/nim/meta/llama-3.1-8b-instruct:1.0.0`).10. **[Preview URL]** After successful deployment, user receives a preview URL (e.g., `https://<random>‐preview.emergentsh.com`) that updates on each redeploy.⸻## Open Questions**1. Which NIM model(s) to default to?**   Recommend starting with `meta/llama-3.1-8b-instruct` (OpenAI‑compatible, low latency).  Optionally allow model selection via settings.  **2. Preview URL mechanism?**   Options: Vercel preview‑deploy API, Netlify Deploy Hooks, or a simple reverse‑proxy + Docker‑in‑Docker sandbox.  Decide during implementation.  **3. Credit granularity?**   Track both input and output tokens via NIM `/v1/chat/completions` response `usage` field.  Decide later whether to charge per‑token or per‑agent‑turn.  **4. Self‑debug loop limits?**   Start with max 3 auto‑fix attempts per agent task; escalate to human‑in‑the‑loop (chat) after that.  **5. Docker‑in‑Docker safety?**   For preview deploys, consider using a sandboxed Docker‑in‑Docker (DinD) runner with time‑ and resource‑limits, or outsource to Vercel/Netlify APIs.⸻## Phase‑Gating (Todos)**}}]}(this);} catch(e) {top.console.log({