# ARCHITECTURE.mdx — Beast-Mode Crypto Trader v1.1 (Design Phase Update) # System Overview (Unchanged) ``` │                         BEAST-MODE TRADING PLATFORM                              │ ├──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │ │  DISCOVERY   │───▶│   SCORING    │───▶│    SAFETY    │───▶│    ENTRY     │  │ │   LAYER      │    │   LAYER      │    │   LAYER      │    │   LAYER      │  │ └──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘  │                                                                                  │  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │ │    EXIT      │◀───│    RISK      │◀───│  OBSERVABILITY │◀───│    ENTRY     │  │ │   LAYER      │    │   LAYER      │    │    LAYER     │    │   LAYER      │  │ └──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘  │                                                                                  │  └─────────────────────────────────────────────────────────────────────────────────┘ ``` # Design Phase Updates - Updated component responsibilities to reflect Beast-Mode enhancements:   - **Discovery Layer**: Now includes multi-source alpha fusion (GMGN signals/clusters/trenches, DexScreener profiles/CTO/boosts, on-chain RPC, ML model) with deduplication and ranking. UI component: Discovery Sources Monitor.   - **Scoring Layer**: Composite score blending gold-score + ML score + Beast discovery score; tiered LEGENDARY/HIGH/MEDIUM with mode qualification (SNIPER/EDGE/EXPLOSIVE). UI components: Beast Score Composite Display, Enhanced Mode Badge.   - **Safety Layer**: 6-surface concentric rug/honeypot/LP gate (authority, LP-lock, holder concentration, honeypot symmetry, wash, creator history) fail-closed by default. UI component: Enhanced Safety Chips.   - **Entry Layer**: Confirmation-gated entry (bp5m, pc5m, vol-mom) with micro-wallet sizing. UI component: Entry Confirmation Signals display.   - **Exit Layer**: Asymmetric exit ladder (1.5x→1000x partials) + tiered trailing stops; never exit moonshot bags on dead-cat signatures above ROCKET tier. UI component: Enhanced Exit Ladder Panel.   - **Risk Layer**: Per-trade max-loss, portfolio heat cap, daily drawdown circuit-breaker. UI component: Enhanced Risk Gauge.   - **Observability Layer**: Structured telemetry for every veto/admit/exit decision (audit trail). UI component: Enhanced Observability Feed.   - **Resilience Layer**: Auto-reconnect for GMGN, Helius, ML, JuPiter RPCs. UI component: Connection status indicators. - Updated data flow to include new data points from Beast-Mode enhancements. - Updated API contracts to include new fields for Beast-Mode features (if needed). # Next Steps (Implementation Phase) - Update backend services to expose new Beast-Mode data points. - Create new frontend components as designed. - Update existing frontend components to display new data. - Ensure data flows correctly from backend to frontend. # End Goal A robust, beast-mode trading platform architecture that supports the enhanced features for maximal win-rate, near-zero rug exposure, and asymmetric moonshot capture.